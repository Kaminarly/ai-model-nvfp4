/* Minimal Python.h fixture for the preflight C-compiler probe (tests only).
   The probe only checks that a compiler can include <Python.h> from the
   include dir; the header content is irrelevant to the check. */
#ifndef PY_PYTHON_H
#define PY_PYTHON_H
#endif
